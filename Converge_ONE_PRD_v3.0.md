# Converge ONE - Product Requirements Document v3.0

> **"The AI Operating System for Modern Work"**

---

## 1. 产品愿景与第一性原理

### 1.1 第一性原理

**用户不需要 AI，用户需要完成工作。**

传统工具的问题：
- 工具分散（Figma、Notion、Slack、Email...），切换成本高
- 每个工具都要学习，认知负担重
- 跨工具协作困难，信息孤岛
- 重复性工作无法自动化

**Converge ONE 的核心命题**：
> 让 AI 成为连接一切的中枢神经，用自然语言驱动专业工具，将意图直接转化为可用成果，并让工作模式可沉淀、可复用。

### 1.2 市场机会

| 产品 | 优势 | 缺失 |
|------|------|------|
| ChatGPT/Claude | 通用智能 | 无专业工具、无结构化输出 |
| Notion AI | 文档集成 | 能力有限、无跨平台 |
| Cursor | 代码深度 | 仅限开发领域 |
| Zapier/Make | 自动化 | 不够智能、配置复杂 |
| NotebookLM | 知识问答 | 输出单一、无工具调用 |

**Converge ONE 的差异化定位**：
- ✅ 通用智能（自然语言理解）
- ✅ 专业工具（App 生态）
- ✅ 结构化输出（Artifact Canvas）
- ✅ 能力沉淀（用户可创建 AI App）
- ✅ 开放集成（MCP 协议）

### 1.3 产品定位

**Converge ONE = AI 时代的操作系统**

不是做"更好的 ChatGPT"，而是做"AI 之上的工作平台"。

核心价值主张：
1. **ONE Interface**：一个界面，完成所有类型的工作
2. **ONE Command**：一句话，调度所有专业能力
3. **ONE Place**：一个地方，沉淀所有知识和工具

---

## 2. 核心使用链路

```
┌─────────┐   ┌─────────────┐   ┌─────────┐   ┌───────────┐   ┌──────────┐   ┌────────┐   ┌────────┐
│ Intent  │ → │Understanding│ → │ Routing │ → │ Execution │ → │ Artifact │ → │ Action │ → │ Memory │
│  意图   │   │    理解     │   │  调度   │   │   执行    │   │   产出   │   │  行动  │   │  沉淀  │
└─────────┘   └─────────────┘   └─────────┘   └───────────┘   └──────────┘   └────────┘   └────────┘
     ↑                                                                                         │
     └─────────────────────────────────────────────────────────────────────────────────────────┘
                                            (反馈循环)
```

### 链路详解

| 阶段 | 说明 | 示例 |
|------|------|------|
| **Intent** | 用户用自然语言表达需求 | "帮我做个产品介绍页" |
| **Understanding** | 系统解析意图、识别能力需求 | 需要设计能力 + 文案能力 |
| **Routing** | 选择合适的 App（内置/自建/第三方） | 调用 Framia (设计) |
| **Execution** | App 执行任务，可能需要多轮交互 | Framia 生成设计稿 |
| **Artifact** | 生成结构化产出，在 Canvas 展示 | 可交互的设计预览 |
| **Action** | 产出可导出、分享、深入编辑 | 一键导出到 Figma |
| **Memory** | 产出归档，流程可保存为 App | 保存为"产品页生成器" |

---

## 3. 产品架构

### 3.1 信息架构 (Information Architecture)

```
CONVERGE ONE
├── 💬 Chat          ← 执行层 (Runtime)
│   ├── Conversation
│   ├── App Invocation (@mention)
│   └── Canvas (Artifact Display)
│
├── 🤖 Apps          ← 能力层 (Factory)
│   ├── My Apps (用户创建)
│   ├── Built-in Apps (系统内置)
│   ├── Connected Apps (第三方集成)
│   └── App Studio (创建/编辑界面)
│
└── 📁 Spaces        ← 数据层 (Storage)
    ├── All Files (统一文件视图)
    ├── Projects (项目文件夹)
    └── Knowledge Bases (RAG 知识库)
```

### 3.2 导航结构

```
┌────────┐
│  LOGO  │
├────────┤
│   💬   │  Chat      ← 日常工作入口
│   🤖   │  Apps      ← 能力管理入口
│   📁   │  Spaces    ← 数据管理入口
├────────┤
│   ⚙️   │  Settings
│   👤   │  Profile
└────────┘
```

**设计原则**：
- 三个一级入口，职责清晰
- Chat 是默认首页（高频使用）
- Apps 独立入口（强调能力创造）
- Spaces 纯粹存储（不混杂功能）

---

## 4. 功能模块详述

### 4.1 Chat（执行层）

**定位**：用户的日常工作台，80% 的交互在这里完成。

#### 4.1.1 核心功能

| 功能 | 说明 |
|------|------|
| **对话输入** | 自然语言输入，支持多模态（文本、图片、文件） |
| **App 调用** | 通过 @mention 精确调用特定 App |
| **智能路由** | 系统自动识别意图，推荐合适的 App |
| **Canvas** | 右侧画布展示结构化 Artifact |
| **上下文管理** | 会话内 App 间共享上下文 |
| **快捷操作** | Quick Actions 快速启动常用任务 |

#### 4.1.2 Canvas 类型

| Artifact 类型 | 展示方式 | 操作 |
|--------------|---------|------|
| 代码 | 语法高亮编辑器 | 复制、运行、导出 |
| 设计稿 | 可交互预览 | 缩放、标注、导出 |
| 文档 | 富文本渲染 | 编辑、导出 |
| 表格 | 数据表格 | 筛选、排序、导出 |
| 幻灯片 | 幻灯片预览 | 翻页、导出 |
| 流程图 | 可视化图表 | 缩放、导出 |

#### 4.1.3 上下文总线 (Context Bus)

```
┌─────────────────────────────────────────────────────────┐
│                    Context Bus                          │
├─────────────────────────────────────────────────────────┤
│  App A 输出 → 结构化数据 → App B 可读取                  │
│                                                         │
│  示例：                                                  │
│  Hunter 研究竞品 → 输出分析报告 → Framia 基于报告设计    │
└─────────────────────────────────────────────────────────┘
```

---

### 4.2 Apps（能力层）

**定位**：能力的创造与管理中心，让每个用户都能成为 AI 工具的创造者。

#### 4.2.1 App 分类

| 类型 | 说明 | 示例 |
|------|------|------|
| **Built-in Apps** | 系统内置的专业 AI Agent | Framia, Enter, Hunter, Combos |
| **My Apps** | 用户创建的自定义 AI App | 周报生成器、竞品分析器 |
| **Connected Apps** | 通过 MCP 协议接入的第三方 | Figma, Slack, Notion |

#### 4.2.2 Built-in Apps

| App | 能力域 | 核心功能 |
|-----|--------|---------|
| **Framia** | 设计 | UI 设计、原型生成、设计系统 |
| **Enter** | 开发 | 代码生成、调试、部署 |
| **Hunter** | 研究 | 信息检索、竞品分析、数据挖掘 |
| **Combos** | 自动化 | 工作流编排、批量处理 |

#### 4.2.3 My Apps（用户自建）

**App 类型**：

| 类型 | 说明 | 适用场景 |
|------|------|---------|
| **Prompt App** | 单一 Prompt + 可选知识库 | 简单任务，如周报生成 |
| **Workflow App** | 多步骤编排 | 复杂流程，如内容发布 |
| **Tool-call App** | 调用外部 API | 集成外部服务 |

**App 配置项**：

```typescript
interface AIApp {
  id: string;
  name: string;
  icon: string;
  description: string;
  
  // 类型与触发
  type: 'prompt' | 'workflow' | 'tool-call';
  trigger: 'manual' | 'schedule' | 'webhook';
  
  // Prompt 配置
  systemPrompt?: string;
  
  // 工作流配置
  steps?: WorkflowStep[];
  
  // 知识库绑定
  knowledgeBaseIds?: string[];
  
  // 连接的 App
  connectedApps?: string[];
}
```

#### 4.2.4 App Studio

**功能**：
- **Prompt Editor**：编写和调试 System Prompt
- **Workflow Canvas**：可视化编排多步骤流程
- **Test Console**：实时测试 App 效果
- **Version History**：版本管理

**界面布局**：
```
┌─────────────────────────────────────────────────────────┐
│  App Studio: 周报生成器                          [Save] │
├──────────────┬──────────────────────────────────────────┤
│              │                                          │
│  Components  │           Canvas / Editor                │
│              │                                          │
│  ├─ Trigger  │   ┌──────────┐    ┌──────────┐          │
│  ├─ Prompt   │   │  Input   │ →  │  Claude  │ →  ...   │
│  ├─ App      │   └──────────┘    └──────────┘          │
│  ├─ Output   │                                          │
│  └─ ...      │                                          │
│              ├──────────────────────────────────────────┤
│              │           Test Console                   │
│              │   > 测试输入...                          │
│              │   < 测试输出...                          │
└──────────────┴──────────────────────────────────────────┘
```

#### 4.2.5 Connected Apps (MCP)

**MCP 协议集成**：

```typescript
interface MCPApp {
  id: string;
  name: string;
  provider: 'native' | 'mcp' | 'custom';
  
  // MCP 特有
  mcpEndpoint?: string;
  capabilities?: string[];
  authConfig?: {
    type: 'oauth' | 'api-key';
    // ...
  };
}
```

**已规划集成**：
- Figma（设计协作）
- Slack（团队沟通）
- Notion（知识管理）
- GitHub（代码仓库）
- Google Drive（文件存储）

---

### 4.3 Spaces（数据层）

**定位**：知识和资产的统一管理，纯粹的存储层。

#### 4.3.1 结构

```
Spaces
├── All Files          ← 所有文件的统一视图
│   ├── Artifacts      ← AI 生成的产出
│   └── Sources        ← 用户上传的素材
│
├── Projects           ← 项目级组织
│   ├── Project A
│   │   ├── artifacts/
│   │   └── sources/
│   └── Project B
│
└── Knowledge Bases    ← RAG 知识库
    ├── KB: 产品文档
    └── KB: 竞品资料
```

#### 4.3.2 All Files

**功能**：
- 统一的文件浏览器
- 支持搜索、筛选、排序
- 支持 Grid/List 视图切换
- 文件预览和快捷操作

**文件类型**：

| 类型 | 来源 | 示例 |
|------|------|------|
| Artifact | AI 生成 | 代码文件、设计稿、文档 |
| Source | 用户上传 | PDF、图片、CSV |

#### 4.3.3 Projects

**功能**：
- 项目级文件夹
- 关联特定会话
- 团队共享（未来）

#### 4.3.4 Knowledge Bases

**功能**：
- 创建专题知识库
- 上传文档建立索引
- RAG 增强的问答
- 绑定到 AI App

**与 NotebookLM 的对标**：

| 功能 | NotebookLM | Converge ONE |
|------|------------|--------------|
| 知识库创建 | ✅ | ✅ |
| 文档上传 | ✅ | ✅ |
| 智能问答 | ✅ | ✅ |
| 生成摘要 | ✅ | ✅ |
| **工具调用** | ❌ | ✅ |
| **生成 Artifact** | ❌ | ✅ |
| **绑定到 App** | ❌ | ✅ |

---

## 5. 用户旅程

### 5.1 新用户首次体验

```
1. 打开 Converge ONE
   ↓
2. 看到 Chat 空状态页
   - 产品价值介绍
   - Quick Actions 引导
   ↓
3. 点击 "Design a landing page"
   ↓
4. 系统自动调用 Framia
   ↓
5. Canvas 展示设计稿
   ↓
6. 用户说 "换个配色"
   ↓
7. Framia 迭代设计
   ↓
8. 用户满意，导出设计稿
   ↓
9. Artifact 自动保存到 Spaces
```

### 5.2 创建自定义 App

```
1. 点击侧边栏 🤖 Apps
   ↓
2. 进入 Apps 页面，看到所有 App
   ↓
3. 点击 [+ Create App]
   ↓
4. 进入 App Studio
   ↓
5. 选择类型：Prompt App
   ↓
6. 编写 System Prompt
   ↓
7. 绑定知识库（可选）
   ↓
8. 在 Test Console 测试
   ↓
9. 点击 [Publish]
   ↓
10. 回到 Chat，@新App 开始使用
```

### 5.3 基于知识库的创作

```
1. 点击侧边栏 📁 Spaces
   ↓
2. 进入 Knowledge Bases
   ↓
3. 创建 "Q1 营销策划" 知识库
   ↓
4. 上传相关 PDF 文档
   ↓
5. 在知识库内提问
   ↓
6. 点击 [Generate Slides]
   ↓
7. 基于回答生成 PPT
   ↓
8. 在 Canvas 预览和编辑
```

---

## 6. 技术架构

### 6.1 App Registry

```typescript
class AppRegistry {
  private apps: Map<string, MCPApp> = new Map();
  
  // 注册 App
  register(app: MCPApp): void;
  
  // 获取所有 App
  getAll(): MCPApp[];
  
  // 按类型获取
  getByProvider(provider: 'native' | 'mcp' | 'custom'): MCPApp[];
  
  // 按能力获取
  getByCapability(capability: string): MCPApp[];
  
  // 智能匹配
  matchByIntent(intent: string): MCPApp[];
}
```

### 6.2 Context Bus

```typescript
interface ContextBus {
  // 发布上下文
  publish(appId: string, data: ArtifactContextData): void;
  
  // 订阅上下文
  subscribe(callback: (data: ArtifactContextData) => void): void;
  
  // 获取会话上下文
  getSessionContext(): ArtifactContextData[];
}
```

### 6.3 MCP 协议

```typescript
interface MCPProtocol {
  // 连接外部服务
  connect(endpoint: string, auth: AuthConfig): Promise<MCPConnection>;
  
  // 调用能力
  invoke(connection: MCPConnection, capability: string, params: any): Promise<any>;
  
  // 断开连接
  disconnect(connection: MCPConnection): void;
}
```

---

## 7. 路线图

### Phase 1: MVP (当前)

**目标**：验证核心假设

| 功能 | 状态 | 优先级 |
|------|------|--------|
| Chat + Canvas | ✅ 已实现 | P0 |
| Built-in Apps (4个) | ✅ 已实现 | P0 |
| Apps 一级入口 | 🔄 进行中 | P0 |
| App Studio (基础版) | 🔄 进行中 | P0 |
| Spaces (All Files) | ✅ 已实现 | P0 |
| Knowledge Bases | ✅ 已实现 | P0 |

### Phase 2: 能力扩展

| 功能 | 说明 |
|------|------|
| MCP 集成 | 接入 Figma, Slack 等 |
| Workflow App | 可视化工作流编排 |
| 高级 Canvas | 更多 Artifact 类型 |
| 版本管理 | App 和 Artifact 版本 |

### Phase 3: 生态建设

| 功能 | 说明 |
|------|------|
| App Marketplace | 社区 App 分享 |
| Team Collaboration | 团队协作功能 |
| API & SDK | 开发者接入 |
| Enterprise | 私有部署 |

---

## 8. 成功指标

### 8.1 北极星指标

**Weekly Active Users (WAU) 中完成至少一个 Artifact 的用户占比**

> 这个指标衡量的是"用户是否真正在用 ONE 完成工作"，而不仅仅是"聊天"。

### 8.2 过程指标

| 指标 | 目标 | 说明 |
|------|------|------|
| DAU/MAU | > 40% | 用户粘性 |
| Artifacts/User/Week | > 5 | 产出效率 |
| Custom Apps Created | > 1/User | 能力沉淀 |
| App Invocation Rate | > 60% | App 使用率 |
| Canvas Interaction Rate | > 70% | Artifact 交互 |

### 8.3 用户满意度

| 指标 | 目标 |
|------|------|
| NPS | > 50 |
| Task Completion Rate | > 80% |
| Time to First Value | < 5 min |

---

## 9. 附录

### 9.1 术语表

| 术语 | 定义 |
|------|------|
| **App** | AI 能力单元，可以是内置、用户自建或第三方 |
| **Artifact** | AI 生成的结构化产出 |
| **Canvas** | 展示 Artifact 的可交互画布 |
| **Context Bus** | App 间共享上下文的机制 |
| **MCP** | Micro-Capability Protocol，第三方集成协议 |
| **Knowledge Base** | RAG 增强的知识库 |
| **Space** | 数据存储单元（项目或知识库） |

### 9.2 竞品参考

- **ChatGPT**：对话交互范式
- **Cursor**：IDE 级别的 AI 集成
- **NotebookLM**：知识库问答
- **Zapier**：工作流自动化
- **Figma**：协作设计工具

